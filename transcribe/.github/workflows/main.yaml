name: Testing
on: push

jobs: 
  test_project:
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2
    - name: Run Django Tests
      run: |
        pip3 install -r requirements.txt
        python3 manage.py tests liveTranscription.tests chatApp.tests transcriptionApp.tests